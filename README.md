# Multiplayer Chess: A Real-Time Web Application

To run this file first download the dpendencies using npm i

Then download the download libraries express, node and socket.io using "npm i express node socket.io"

Now start the server using "node socket.js" and then render the index.file
